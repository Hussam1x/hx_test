#include<stdexcept>
using namespace std; 

#include "dector.h"
void DVector::copy(const DVector &vct)
{
   cpct = vct.cpct;
   numb = vct.numb;
   ptr-arr = new int [cpct];
   for (int i = 0, i < numb, i++)
   {
      ptr-arr
   }


} 



//default constructor 
DVector::DVector();
cpct(0), 
numb(0),
ptr-arr(nullptr)

{
}//DVector

//destructor
DVector::~DVector()
{

   free()
}//DVector
